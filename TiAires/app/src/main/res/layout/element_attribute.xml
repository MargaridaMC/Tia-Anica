<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="@dimen/margin">

    <ImageView
        android:id="@+id/attribute_icon"
        android:layout_width="@dimen/large_icon_size"
        android:layout_height="@dimen/large_icon_size"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:id="@+id/attribute_text"
        app:layout_constraintTop_toTopOf="@+id/attribute_icon"
        app:layout_constraintStart_toEndOf="@+id/attribute_icon"
        app:layout_constraintBottom_toBottomOf="@+id/attribute_icon"
        android:layout_marginStart="@dimen/margin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/TextAppearance.AppCompat.Title"/>

</androidx.constraintlayout.widget.ConstraintLayout>